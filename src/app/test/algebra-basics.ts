export function addPositiveNumbers(input1: number, input2: number) {
  if (input1 > 0 && input2 > 0) {
    return input1 + input2;
  }
  return 0;
}

export function multiplyNumbers(input1: number, input2: number) {
  return input1 * input2;
}
